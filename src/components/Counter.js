import React from 'react';const Counter = (props) => {    return (        <div>            <button id="btn-minus" onClick={() => props.calculate(props.el.id, -props.el.value)}>-{props.el.value}</button>            <button id="btn-plus" onClick={() => props.calculate(props.el.id, props.el.value)}>+{props.el.value}</button>            <button id="btn-delete" onClick={()=>props.deleteCounter(props.el.id)}>X</button>        </div>    );};export default Counter;